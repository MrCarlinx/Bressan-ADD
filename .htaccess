# Bloqueia acesso direto a arquivos sensíveis na raiz
<FilesMatch "^(\.env|\.env\.example|database\.sql)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</FilesMatch>
